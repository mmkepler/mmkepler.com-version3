import React from 'react'
import * as headerStyles from "../styles/header.module.css"

export default function Header() {
  return (
    <header id="header" className={headerStyles.header}>Header</header>
  )
}
